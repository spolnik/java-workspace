apply from: "$rootDir/libraries.gradle"

allprojects {
    apply plugin: 'idea'
}

subprojects {
    repositories {
        mavenCentral()
    }

    apply plugin: 'java'

    sourceCompatibility = 1.8
    version = '1.0'

    dependencies {
        testCompile 'junit:junit'
    }
}
